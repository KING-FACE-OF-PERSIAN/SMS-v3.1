# به‌روزرسانی Repository به نسخه ۱.۳

1. ZIP نسخه ۱.۳ را Extract کن.
2. در ریشه Repository گیت‌هاب، `Add file` و سپس `Upload files` را بزن.
3. پوشه `app` و فایل‌های `README_FA.md`، `CHANGES_V1.3_FA.md` و `UPDATE_V1.3_FA.md` را داخل کادر Upload بکش.
4. روی `Commit changes` بزن.
5. در تب `Actions` منتظر پایان جدیدترین اجرای `Build Android APK` بمان.
6. Artifact جدید `ConsentSMS-debug-apk` را دانلود و Extract کن.
7. اگر نصب روی نسخه قبلی انجام نشد، نسخه قبلی را حذف و APK جدید را نصب کن.

پوشه `.github` نیازی به بارگذاری دوباره ندارد.
